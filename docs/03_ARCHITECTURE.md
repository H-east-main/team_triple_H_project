# 기술 스택

Frontend: Vue 3 + Vite
Backend: FastAPI
Database: SQLite
Map: Kakao Map API

# 원칙

- 프론트는 공공데이터 JSON 파일에 직접 접근하지 않는다.
- 모든 데이터는 백엔드 API를 통해 받는다.
- 상태 관리는 현재 컴포넌트 상태와 Pinia만 사용한다.
- 새로운 UI 라이브러리는 임의로 추가하지 않는다.
- 기존 디렉터리 구조는 통합 담당자의 승인 없이 변경하지 않는다.